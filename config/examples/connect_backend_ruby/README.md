# connect-backend-ruby

Example Ruby backend for pgport Connect feature

## Usage

Install and run the backend:

```bash
bundle install
ruby main.rb
```

Configure pgport:

```bash
pgport --sessions --connect-backend=http://localhost:4567 --connect-token=test
```
